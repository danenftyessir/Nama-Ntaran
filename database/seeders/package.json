{
  "name": "mbg-seeders",
  "version": "1.0.0",
  "description": "Seeding scripts for MBG (Makanan Bergizi Gratis) database",
  "scripts": {
    "seed:users": "ts-node 01-seed-users.ts",
    "seed:menu-items": "ts-node 02-seed-menu-items.ts",
    "seed:deliveries": "ts-node 04-seed-deliveries.ts",
    "seed:allocations": "ts-node 03-seed-allocations.ts",
    "seed:verifications": "ts-node 05-seed-verifications.ts",
    "seed:all": "npm run seed:users && npm run seed:menu-items && npm run seed:deliveries && npm run seed:allocations && npm run seed:verifications"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "bcrypt": "^5.1.1",
    "csv-parser": "^3.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^20.10.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
